export const DASHBOARD_PATH = '/dashboard';

const config = {
  fontFamily: `'Roboto', sans-serif`,
  borderRadius: 8,
  defaultPath: '/dashboard',
  fontFamily: `'Roboto', sans-serif`,
  i18n: 'en',
  miniDrawer: false,
  container: true,
  mode: 'light',
  presetColor: 'default',
  baseApi: 'http://localhost:5009/api',
  baseUrl: 'http://localhost:3009',


};
export const drawerWidth = 280;
export default config;
